/* Given two lists 'a' and 'b' that have the same tracks but in a different order, find the index mapping from 'a' to 'b'. An index mapping means that the ith element in 'a' appears at the jth element in 'b'.

Example:
a = ['good kid', 'Money Trees', 'Compton', 'Poetic Justice', 'Real'];
b = ['Money Trees', 'Real', 'Compton', 'good kid', 'Poetic Justice'];

=> return [3, 0, 2, 4, 1]

  Index Mapping:
  'good kid'       = index 0 in 'a', index 3 in 'b'
  'Money Trees'    = index 1 in 'a', index 0 in 'b'
  'Compton'        = index 2 in 'a', index 2 in 'b'
  'Poetic Justice' = index 3 in 'a', index 4 in 'b'
  'Real'           = index 4 in 'a', index 1 in 'b'
*/

/**
 * @param  {array} a
 * @param  {array} b
 * @return {array}
 */

const findIndexMapping = (a, b) => {};

export default findIndexMapping;
